# 缘梦婚纱 · AI 客服平台 — 工作原则

## 项目定位

AI-Native 客服平台。AI 不只是客服工具，它是系统本身的运营界面。
管理员通过与 AI 对话来配置系统，而不是填表单或改代码。

---

## 一、数据可靠性（硬性要求）

**AI 只负责交互、路由、语言格式化。不生成任何业务事实。**

- 订单信息、商品价格、退款比例、政策条款 → 必须来自数据库
- 检索无结果时：明确失败（handoff）> 低质量半对答案
- 所有文档必须有 embedding，写入后同步生成，失败则阻止或回滚
- BM25 是辅助 boost，对中文效果差是已知局限，不用传统 NLP 工具弥补

**不引入 workaround，修复根因。**

---

## 二、设计原则

### 业务优先
- UI 按真实业务流程组织：准备阶段 → 日常运营 → 日终复盘
- 不按数据类型组织（不是 CRUD 表格集合）

### AI-Native Harness
- 结构化数据面板自动向 AI 注入上下文（区块 mount 时调用 `injectContext`）
- AI 在有边界的工作场内操作，不自由发挥
- 用户的自然语言指令通过工具调用完成实际 DB 操作，工具调用对管理员透明

### Toolchains（工具链）
- 多步原子操作合并为单一高阶动作
- 示例：创建文档 → 嵌入 → 刷新 BM25 = 一个按钮，不是三步操作

### 无缝体验，不冷拒绝
- AI 无法可靠回答时：生成交接摘要 → 温和过渡语 → 转人工
- 客户侧感受是"被连接到专家"，不是"被系统拒绝"

---

## 三、实施方法

### 改动前
1. **审计先行**：精确定位现状（文件路径、行号、数据库字段）
2. **业务价值排序**：P0 → P1 → P2，不因技术有趣而改变顺序
3. **精确到行**：每个改动计划必须包含具体文件、行号、改法

### 改动中
4. **实施顺序**：后端 → 测试 → 前端 → E2E 验证，不跳步
5. **向后兼容**：新参数加默认值（`extra_config=None`），现有调用不受影响
6. **零新依赖**：已有工具够用则不引入新包

### 改动后
7. **信任但验证**：报告"实际发生了什么"，不只是"我做了什么"
8. **E2E 确认**：单元测试绿 + 浏览器实际操作验证，两者缺一不可
9. **不主动 commit**：除非用户明确要求

---

## 四、代码约束

```
src/ai_customer_service/   — Python 后端（LangGraph + FastAPI）
workspace/src/             — React 前端（TypeScript）
tests/                     — 所有测试（uv run pytest tests/ -q）
scripts/migrate_db.py      — DB schema + 种子数据（唯一入口）
```

- 图节点改动必须附带对应单元测试
- 前端纯 UI 改动手动 E2E 验证即可
- DB schema 变更统一在 `migrate_db.py`，不散落在代码里
- 节点从 `config["configurable"]` 读服务，不直接访问 DB

---

## 五、关键架构决策（不可随意更改）

| 决策 | 原因 |
|------|------|
| BusinessProfile 配置驱动图构建 | 切换业务只改 DB，不改代码 |
| LangGraph `interrupt()` 实现 HITL | 状态持久化 + 人工审批原子性 |
| Socket.IO singleton（token 先于模块加载） | 避免重复连接，token 必须在 import 前写入 localStorage |
| 知识库分级：business_policy 严格 / industry_knowledge 灵活 | 政策类不允许 LLM 补充，行业常识可补充 |
| 所有节点 fallback：profile 为 None 时使用 hardcoded 值 | 兼容无 profile 的测试环境 |

---

## 六、体验细节原则

技术完整性和体验完整性同等重要：

- **等待反馈**：多工具调用期间必须有进度提示（`bot_progress`），5-8s 无反馈等于体感卡顿
- **行动可点击**：AI 提到"可以申请退货"时，旁边必须有按钮，不让用户手打
- **决策可追溯**：AI 回复来源对客服可见（知识库文档/订单/商品），一键核查
- **分诊效率**：会话列表卡片显示意图标签，客服 3 秒内判断优先级

---

## 七、禁止项

- ❌ 在没有 DB 数据支撑的情况下让 LLM 生成商品名/价格/政策条款
- ❌ 为掩盖根因问题引入传统 NLP 补丁（jieba、规则匹配等）
- ❌ 在 socket 工具结果中执行任何未经用户确认的指令
- ❌ 未经明确要求就 commit、push 或删除文件
- ❌ 引入新的第三方依赖（除非经过讨论并明确批准）
